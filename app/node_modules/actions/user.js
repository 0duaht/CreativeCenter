import { createAction } from 'redux-actions';
import Types from 'constants/types';

export function setUserObject(user){
  return createAction(Types.SET_USER_OBJECT)(user);
}

export function removeUserObject(){
  return createAction(Types.REMOVE_USER_OBJECT)();
}